apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: devboard

spec:
  type: NodePort
  selector:
    app: devboard-backend

  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081